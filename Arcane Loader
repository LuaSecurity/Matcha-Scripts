local Arcane = {}

local Ugc = game
local Drawing = Drawing

local PlaceId = Ugc.PlaceId
local JobId = Ugc.JobId

local Players = Ugc.Players
local Workspace = Ugc.Players

local LocalPlayer = Players.LocalPlayer
local PlrBackpack = LocalPlayer.Backpack
local PlrPlayerGui = LocalPlayer.PlayerGui

local IsSupported = nil

local SupportedGames = {

	[142823291] = "Murder Mystery 2",
	[87812856808709] = "Arcane Hub Testing Place"

}

local function GetSupportedGame(PlaceId)
	if not PlaceId then
		return
	end

	if not SupportedGames[PlaceId] then
		return "Unsupported game."
	end

	return true or false
end

IsSupported = GetSupportedGame(PlaceId)

if IsSupported ~= true then print(IsSupported) return end

print('Supported game found, Loading Arcane Hub...')

wait(2)
print("Loaded Arcane Hub!")
return Arcane
